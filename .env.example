# Copy this file to .env and fill in real values. .env is gitignored — never commit real credentials.
BASE_URL=https://flagman.ua/
# local phone format, no country code - the field masks it as +38(0XX)XXX-XX-XX
VALID_PHONE=0000000000
VALID_PASSWORD=change-me
INVALID_PASSWORD=not-the-real-password
